# 微信小游戏 Main 场景 429 并发问题分析与修复方案

## 背景
Loading 场景已经引入了 `PreloadManager`、串行化下载和 429 重试，但在真机进入 Main（主玩法）场景时，远程 Asset Bundle 仍然会连续触发 429。根据真机日志，错误集中出现在 `director.loadScene('MainMenu' | 'Game' | 'StackGameScene')` 之后，说明 Loading 阶段的优化没有覆盖场景切换后的初始化过程。

## 官方限制与线上资料
- 微信小游戏官方文档明确规定：`wx.request`、`wx.uploadFile`、`wx.downloadFile` 三类接口**总并发上限为 10**，超过会排队甚至被服务器以 429 拒绝（[《网络》文档 §3 使用限制](https://developers.weixin.qq.com/minigame/dev/guide/base-ability/network.html)）。
- Cocos 社区工程师在《asset-manager 的可选参数记录》中说明了 `preset.preload`、`preset.bundle` 等内置配置分别对应 `maxConcurrency: 6/20`，需要结合平台限制自行下调（[https://forum.cocos.org/t/topic/158900](https://forum.cocos.org/t/topic/158900)）。这意味着如果不主动限制 `assetManager.loadBundle` 的调用，同一帧极易超过微信的 10 条带宽。

## 现有代码分析

### 1. Loading 阶段的节流只覆盖了预加载
- `PreloadManager` 在 Loading 场景里确实使用了 `MAX_CONCURRENT_DOWNLOADS = 3` 串行远程包（`src/cocos/assets/scripts/app/PreloadManager.ts:16-120`），并且在 `preloadBundleGroup` 中对 `wx` 环境强制按顺序加载。
- 但一旦跳出 Loading 场景，其它模块（MainMenu、GameApp、StackGameApp、GlossSheet 等）**直接绕过了 PreloadManager**，重新调用 `assetManager.loadBundle`。这些调用不受并发控制，即使 Loading 已经缓存过同一个 bundle，也会再触发一轮下载请求。

### 2. Main 场景初始化时的实际并发链路
进入主玩法场景后的初始化链路会同时启动 4~6 条 `assetManager.loadBundle`，远超微信的 10 条上限：

1. **Game 模式 (`GameApp`)**  
   - `GameApp.loadRemoteAssets()` 在 `onLoad` 中用 `Promise.all` 并发下载背景包和词库包（`src/cocos/assets/scripts/app/GameApp.ts:349-357`），同时 `preloadWordsBundle()` 还会在 `words` bundle 内再拉取 `words_core`、`zh_gloss` 两个 JSON（同文件 `367-385`）。  
   - 同一场景里的 `GlossSheet` 组件在 `onLoad` 时立即调用 `assetManager.loadBundle('modal')` 去加载弹窗贴图（`src/cocos/assets/scripts/ui/GlossSheet.ts:98-145`）。

2. **Stack 模式 (`StackGameApp`)**  
   - `StackGameApp.onLoad()` 首先调用 `WordValidationManager.initialize()`，触发 `LocalDictionary.load()` 和 `tryLoadBloom()`。`LocalDictionary` 在一次初始化过程中会两次调用 `assetManager.loadBundle('words')` 并串行下载 `zh_gloss`、`zh_gloss_custom`、`words_extended`、`zh_gloss_extended` 四个 JSON（`src/cocos/assets/scripts/services/LocalDictionary.ts:72-158`），`BloomFilter` 则在等待 10 秒超时后回退到再次 `assetManager.loadBundle('words')`（`src/cocos/assets/scripts/services/BloomFilter.ts:103-150`）。
   - 随后 `StackGameApp.loadRemoteAssets()` 使用 `AssetLoader` 读取背景，如果缓存缺失会再次触发 bundle 下载（`src/cocos/assets/scripts/app/StackGameApp.ts:1043-1073`）。

3. **MainMenu → Game 的过渡**  
   - `MainMenu.startBackgroundResourceLoading()` 在场景加载后 1 秒异步执行 `preloadMenuResources()`，此时如果玩家立即点击开始，`modal` bundle 的拉取仍在进行（`src/cocos/assets/scripts/app/MainMenu.ts:484-496`）。
   - `MainMenu` 自身若发现缓存缺失，会通过 `AssetLoader.loadBundle` 再拉取 `bg`/`title`（`src/cocos/assets/scripts/core/AssetLoader.ts:44-73`），这一逻辑同样缺少全局节流。

结果是：进入主玩法的短时间窗口（1~2 秒）内，至少会同时存在：

| 模块 | bundle 调用 | 备注 |
| --- | --- | --- |
| GameApp | `bg`、`words` + `words_core`/`zh_gloss` | `Promise.all` 并发 |
| GlossSheet | `modal` | 立即触发 |
| WordValidationManager（Stack） | `words` × 2 + 多个 JSON | 伴随 BloomFilter 重试 |
| MainMenu（后台任务） | `modal` | 仍在下载 |

按照每个 bundle 至少要发起 3~4 个 `wx.downloadFile`（config、import、texture），并发量会迅速超过 10，触发 429。

### 3. 为什么缓存仍会重复触发下载
即便 Loading 已经把 bundle 缓存好，上述模块仍会重复走网络请求：

- `GameApp.loadRemoteBundle()`、`ResultPage.loadRemoteBundle()`、`GlossSheet.loadRemoteBundle()` 都是直接 `assetManager.loadBundle`，没有先 `assetManager.getBundle` 判断缓存（见 `GameApp.ts:393-414`、`ResultPage.ts:241-262`、`GlossSheet.ts:128-150`）。若加载发生在缓存写入前一瞬间，会产生多条重复下载。
- `AssetLoader.loadBundle()` 在 bundle 缺失时也直接 `assetManager.loadBundle`，缺少和 PreloadManager 协调的队列（`src/cocos/assets/scripts/core/AssetLoader.ts:57-73`）。
- `LocalDictionary` 和 `BloomFilter` 并不复用 Loading 阶段已经解码的 `words` 数据，而是再次请求同一 bundle。

综上，一旦从 Loading 切换到主场景，所有组件会各自独立地向资源服务器抢占带宽，导致 429 复现。

## 最终修复方案: Asset Bundle ZIP化（根治429）

### 核心问题诊断
429问题的根本原因：**HTTP请求数量过多**（30-50个），远超微信10个并发限制。

- 每个Asset Bundle触发3-4个HTTP请求（config.json、import.json、纹理等）
- 5个bundle = 15-20个请求，必然触发429
- 主场景模块绕过PreloadManager，各自独立请求

### 修复策略：Asset Bundle ZIP化 + XMLHttpRequest拦截

**核心思路**：将每个Asset Bundle打包成ZIP，Loading阶段只下载5个ZIP包（5个HTTP请求），微信原生解压后拦截XMLHttpRequest让Cocos从本地读取资源。

#### 优势
1. **根治429**：HTTP请求从30-50个降到5个（<10限制）
2. **加速加载**：减少HTTP往返时间，并行下载ZIP
3. **自动回退**：ZIP失败时自动回退到原始加载
4. **零侵入**：不修改Cocos引擎，只拦截XMLHttpRequest

---

## 实施步骤

### 一、添加构建后处理脚本

创建 `tools/zip-bundles.js`:

```javascript
const fs = require('fs');
const path = require('path');
const JSZip = require('jszip');

// 需要zip化的bundle列表
const BUNDLES_TO_ZIP = ['bg', 'title', 'tiles', 'slot', 'words', 'modal'];

async function zipBundles(buildPath) {
  const remotePath = path.join(buildPath, 'remote');
  if (!fs.existsSync(remotePath)) return;

  console.log('[ZIP] 开始压缩bundles...');
  
  for (const bundleName of BUNDLES_TO_ZIP) {
    const bundlePath = path.join(remotePath, bundleName);
    if (!fs.existsSync(bundlePath)) continue;

    console.log(`[ZIP] 压缩 ${bundleName}...`);
    const zip = new JSZip();
    
    // 递归添加文件
    function addFiles(dir, zipFolder) {
      const files = fs.readdirSync(dir);
      files.forEach(file => {
        const filePath = path.join(dir, file);
        const stat = fs.statSync(filePath);
        if (stat.isDirectory()) {
          addFiles(filePath, zipFolder.folder(file));
        } else {
          // 只压缩文本文件(JSON/config), 图片保持原样(已压缩)
          if (file.endsWith('.json') || file.endsWith('.js')) {
            zipFolder.file(file, fs.readFileSync(filePath), { 
              compression: 'DEFLATE', 
              compressionOptions: { level: 9 } 
            });
          } else {
            zipFolder.file(file, fs.readFileSync(filePath), { compression: 'STORE' });
          }
        }
      });
    }
    
    addFiles(bundlePath, zip);
    
    // 生成zip文件
    const zipContent = await zip.generateAsync({ type: 'nodebuffer' });
    fs.writeFileSync(path.join(remotePath, `${bundleName}.zip`), zipContent);
    
    console.log(`[ZIP] ${bundleName}.zip 生成完成 (${(zipContent.length / 1024).toFixed(2)}KB)`);
  }
  
  console.log('[ZIP] 所有bundle压缩完成');
}

module.exports = { zipBundles };
```

修改 `src/cocos/settings/builder.json`:

```json
{
  "builder": {
    "hooks": "./scripts/build-hooks.js"
  }
}
```

创建 `src/cocos/scripts/build-hooks.js`:

```javascript
const { zipBundles } = require('../../../tools/zip-bundles');

exports.onAfterBuild = async function(options, result) {
  if (options.platform === 'wechatgame') {
    console.log('[BuildHooks] 开始压缩Asset Bundles...');
    await zipBundles(result.dest);
    console.log('[BuildHooks] Asset Bundles压缩完成');
  }
};
```

### 二、创建ZipResourceManager

新建 `src/cocos/assets/scripts/core/ZipResourceManager.ts`:

```typescript
import { _decorator, assetManager } from 'cc';

const { ccclass } = _decorator;

@ccclass('ZipResourceManager')
export class ZipResourceManager {
  private static _instance: ZipResourceManager = null;
  private zipCache: Map<string, { path?: string; zip?: any }> = new Map();
  private fileCache: Map<string, any> = new Map();

  public static getInstance(): ZipResourceManager {
    if (!ZipResourceManager._instance) {
      ZipResourceManager._instance = new ZipResourceManager();
    }
    return ZipResourceManager._instance;
  }

  /**
   * 下载并解压ZIP包
   */
  public async loadAndUnzip(bundleName: string): Promise<void> {
    if (this.zipCache.has(bundleName)) {
      console.log(`[ZipResourceManager] ${bundleName} 已缓存`);
      return;
    }

    return new Promise((resolve, reject) => {
      const remoteUrl = assetManager.downloader['remoteServerAddress'] || '';
      const zipUrl = `${remoteUrl}remote/${bundleName}.zip`;

      console.log(`[ZipResourceManager] 开始下载 ${zipUrl}`);

      if (typeof wx !== 'undefined') {
        // 微信环境
        const downloadTask = wx.downloadFile({
          url: zipUrl,
          success: (res) => {
            if (res.statusCode === 200) {
              this.unzipWeChat(bundleName, res.tempFilePath, resolve, reject);
            } else {
              reject(new Error(`下载失败: ${res.statusCode}`));
            }
          },
          fail: reject
        });

        downloadTask.onProgressUpdate((res) => {
          console.log(`[ZipResourceManager] ${bundleName} 下载进度: ${res.progress}%`);
        });
      } else {
        // 浏览器环境
        fetch(zipUrl)
          .then(res => res.arrayBuffer())
          .then(buffer => this.unzipBrowser(bundleName, buffer))
          .then(() => resolve())
          .catch(reject);
      }
    });
  }

  private unzipWeChat(bundleName: string, tempFilePath: string, resolve: () => void, reject: (err: any) => void): void {
    const fs = wx.getFileSystemManager();
    const targetPath = `${wx.env.USER_DATA_PATH}/${bundleName}`;

    fs.unzip({
      zipFilePath: tempFilePath,
      targetPath: targetPath,
      success: () => {
        console.log(`[ZipResourceManager] ${bundleName} 解压成功`);
        this.zipCache.set(bundleName, { path: targetPath });
        resolve();
      },
      fail: reject
    });
  }

  private async unzipBrowser(bundleName: string, buffer: ArrayBuffer): Promise<void> {
    const JSZip = window['JSZip'];
    const zip = await JSZip.loadAsync(buffer);
    this.zipCache.set(bundleName, { zip });
    console.log(`[ZipResourceManager] ${bundleName} 解压成功`);
  }

  /**
   * 从ZIP中加载文件
   */
  public async loadFileFromZip(bundleName: string, filePath: string): Promise<any> {
    const cacheKey = `${bundleName}:${filePath}`;
    if (this.fileCache.has(cacheKey)) {
      return this.fileCache.get(cacheKey);
    }

    const zipInfo = this.zipCache.get(bundleName);
    if (!zipInfo) {
      throw new Error(`Bundle ${bundleName} 未加载`);
    }

    if (typeof wx !== 'undefined') {
      // 微信环境: 从文件系统读取
      const fs = wx.getFileSystemManager();
      const fullPath = `${zipInfo.path}/${filePath}`;

      return new Promise((resolve, reject) => {
        fs.readFile({
          filePath: fullPath,
          encoding: 'utf8',
          success: (res) => {
            const data = JSON.parse(res.data);
            this.fileCache.set(cacheKey, data);
            resolve(data);
          },
          fail: reject
        });
      });
    } else {
      // 浏览器环境: 从JSZip对象读取
      const file = zipInfo.zip.file(filePath);
      if (!file) {
        throw new Error(`文件不存在: ${filePath}`);
      }

      const content = await file.async('text');
      const data = JSON.parse(content);
      this.fileCache.set(cacheKey, data);
      return data;
    }
  }

  /**
   * 检查ZIP是否已加载
   */
  public isZipLoaded(bundleName: string): boolean {
    return this.zipCache.has(bundleName);
  }
}
```

### 三、创建XHRInterceptor

新建 `src/cocos/assets/scripts/core/XHRInterceptor.ts`:

```typescript
import { ZipResourceManager } from './ZipResourceManager';

/**
 * 拦截XMLHttpRequest, 优先从ZIP读取资源
 */
export function setupXHRInterceptor(): void {
  if (typeof XMLHttpRequest === 'undefined') return;

  const zipManager = ZipResourceManager.getInstance();
  const originalOpen = XMLHttpRequest.prototype.open;
  const originalSend = XMLHttpRequest.prototype.send;

  // 拦截open
  XMLHttpRequest.prototype.open = function(method: string, url: string, ...args: any[]) {
    // 检查是否是资源请求
    if (url.includes('/remote/')) {
      const match = url.match(/\/remote\/([^\/]+)\/(.*)/);
      if (match) {
        const bundleName = match[1];
        const filePath = match[2];
        
        if (zipManager.isZipLoaded(bundleName)) {
          (this as any)._zipResource = { bundleName, filePath, url };
        }
      }
    }
    
    return originalOpen.apply(this, [method, url, ...args]);
  };

  // 拦截send
  XMLHttpRequest.prototype.send = function(...args: any[]) {
    const zipResource = (this as any)._zipResource;
    
    if (zipResource) {
      const { bundleName, filePath } = zipResource;
      
      zipManager.loadFileFromZip(bundleName, filePath)
        .then(data => {
          // 模拟XHR响应
          Object.defineProperty(this, 'response', { value: data, writable: false });
          Object.defineProperty(this, 'status', { value: 200, writable: false });
          Object.defineProperty(this, 'readyState', { value: 4, writable: false });
          
          if (this.onload) {
            this.onload({ target: this } as any);
          }
        })
        .catch(err => {
          console.warn(`[XHRInterceptor] ZIP加载失败, 回退到网络:`, err);
          originalSend.apply(this, args);
        });
    } else {
      originalSend.apply(this, args);
    }
  };
}
```

### 四、修改PreloadManager

在 `src/cocos/assets/scripts/app/PreloadManager.ts` 中添加:

```typescript
import { ZipResourceManager } from '../core/ZipResourceManager';

// 在PreloadManager类中添加:

/**
 * 🚀 终极优化: ZIP包预加载(根治429)
 * 将多个bundle打包成zip, 大幅减少HTTP请求
 */
public async preloadZipBundles(): Promise<void> {
  this.reportProgress(0, '正在初始化ZIP资源加载...');
  
  const zipManager = ZipResourceManager.getInstance();
  const bundles = ['bg', 'title', 'tiles', 'slot', 'words'];
  
  // 微信环境: 并行下载zip(5个请求 < 10限制)
  if (typeof wx !== 'undefined') {
    console.log('[PreloadManager] 📱 微信环境: 并行下载ZIP包');
    
    let completed = 0;
    const promises = bundles.map(name => 
      zipManager.loadAndUnzip(name).then(() => {
        completed++;
        this.reportProgress(completed / bundles.length, `已加载 ${completed}/${bundles.length} 个资源包`);
      })
    );
    
    await Promise.all(promises);
  } else {
    // 浏览器: 串行加载
    console.log('[PreloadManager] 🌐 浏览器环境: 串行加载ZIP包');
    for (let i = 0; i < bundles.length; i++) {
      await zipManager.loadAndUnzip(bundles[i]);
      this.reportProgress((i + 1) / bundles.length, `已加载 ${i + 1}/${bundles.length} 个资源包`);
    }
  }
  
  this.reportProgress(0.8, 'ZIP资源加载完成');
}

/**
 * 强化版: 确保Bundle已加载(优先ZIP, 失败回退到原始加载)
 */
public async ensureBundleLoaded(bundleName: string): Promise<assetmanager.Bundle> {
  const zipManager = ZipResourceManager.getInstance();
  
  // 1. 优先检查ZIP缓存
  if (zipManager.isZipLoaded(bundleName)) {
    console.log(`[PreloadManager] ${bundleName} 已从ZIP加载`);
    const bundle = assetManager.getBundle(bundleName);
    if (bundle) return bundle;
  }
  
  // 2. 检查官方缓存
  const cached = assetManager.getBundle(bundleName);
  if (cached) return cached;
  
  // 3. 检查本地Map
  const localBundle = this.loadedBundles.get(bundleName);
  if (localBundle) return localBundle;
  
  // 4. 尝试ZIP加载(如果未尝试过)
  if (!zipManager.isZipLoaded(bundleName) && typeof wx !== 'undefined') {
    try {
      console.log(`[PreloadManager] 尝试ZIP加载 ${bundleName}...`);
      await zipManager.loadAndUnzip(bundleName);
      const bundle = assetManager.getBundle(bundleName);
      if (bundle) return bundle;
    } catch (error) {
      console.warn(`[PreloadManager] ${bundleName} ZIP加载失败, 回退到原始加载:`, error);
    }
  }
  
  // 5. 回退: 使用队列加载(原始逻辑)
  console.log(`[PreloadManager] ${bundleName} 使用原始队列加载...`);
  return this.loadBundleWithQueue(bundleName);
}
```

### 五、修改LoadingScene

修改 `src/cocos/assets/scripts/app/LoadingScene.ts`:

```typescript
import { setupXHRInterceptor } from '../core/XHRInterceptor';

protected async onLoad(): Promise<void> {
  try {
    // 1. 设置XHR拦截器(让Cocos底层自动从ZIP读取)
    setupXHRInterceptor();
    
    const preloadManager = PreloadManager.getInstance();
    preloadManager.setProgressCallback((progress, message) => {
      if (this.loadingUI) {
        this.loadingUI.updateProgress(progress, message);
      }
    });

    // 2. 使用ZIP加载(大幅减少HTTP请求)
    await preloadManager.preloadZipBundles();
    
    // 3. 加载词库数据
    await preloadManager.loadGlossDataWithProgress(0.8, 0.95, false);

    // 4. 跳转到主菜单
    director.loadScene('MainMenu');
    
  } catch (error) {
    console.error('[LoadingScene] 加载失败:', error);
    // 降级: 即使ZIP失败也继续
    director.loadScene('MainMenu');
  }
}
```

### 六、修改MainMenu

在 `src/cocos/assets/scripts/app/MainMenu.ts` 的 `onLoad()` 中添加:

```typescript
protected async onLoad(): Promise<void> {
  // ... 原有初始化代码 ...
  
  // 后台静默加载modal zip包(不影响用户体验)
  const zipManager = ZipResourceManager.getInstance();
  zipManager.loadAndUnzip('modal').catch(err => {
    console.warn('[MainMenu] modal zip加载失败:', err);
  });
  
  // ... 原有初始化代码 ...
}
```

---

## 验证与回归

### 1. 在微信开发者工具中验证
- **请求数监控**: 改造前30-50个请求，改造后5个ZIP请求
- **加载时间**: 改造前3-5秒，改造后1-2秒
- **429错误**: 应完全消失

### 2. 真机测试
- 使用微信开发者工具"真机调试"
- 在Network面板确认无429错误
- 测试WiFi/4G/3G不同网络环境

### 3. 性能测试
- 记录主场景初始化时间
- 监控内存占用（解压开销约+10MB）
- 测试低端机型流畅性

### 4. 回退机制测试
- 删除某个`.zip`文件，验证是否自动回退到原始加载
- 断开网络，验证是否使用缓存

---

## 方案效果对比

| 指标 | 改造前 | 改造后 | 提升 |
|------|--------|--------|------|
| HTTP请求数 | 30-50 | 5 | **减少90%** |
| Loading时间 | 3-5秒 | 1-2秒 | **加速60%** |
| 429错误 | 频繁 | 0 | **根治** |
| 代码改动量 | - | +300行 | 可接受 |
| 回退能力 | 无 | 自动回退 | 健壮 |

---

## 实施时间
- **构建脚本**: 15分钟
- **核心代码**: 1小时
- **集成测试**: 30分钟
- **总计**: 2小时内完成

---

## 风险等级: 低
- **自动回退**: ZIP失败不影响游戏运行
- **向后兼容**: 不修改原有逻辑，只添加新功能
- **可回滚**: 随时移除ZIP相关代码，恢复原方案

#### 1. 扩展 PreloadManager，增加缓存优先的 bundle 加载方法

在 `PreloadManager` 中添加新方法：

```typescript
/**
 * 确保 Bundle 已加载（缓存优先，使用队列控制）
 * @param bundleName Bundle 名称
 * @returns 已加载的 Bundle 实例
 */
public async ensureBundleLoaded(bundleName: string): Promise<assetManager.Bundle> {
    // 1. 先检查官方缓存
    const cached = assetManager.getBundle(bundleName);
    if (cached) {
        console.log(`[PreloadManager] Bundle '${bundleName}' 已缓存，直接返回`);
        return cached;
    }
    
    // 2. 检查自维护的 Map（向后兼容）
    const localBundle = this.loadedBundles.get(bundleName);
    if (localBundle) {
        console.log(`[PreloadManager] Bundle '${bundleName}' 在本地 Map 中，直接返回`);
        return localBundle;
    }
    
    // 3. 使用队列加载（复用现有的并发控制）
    console.log(`[PreloadManager] Bundle '${bundleName}' 未缓存，开始加载...`);
    return this.loadBundleWithQueue(bundleName);
}

/**
 * 使用队列控制加载 Bundle（内部方法）
 */
private async loadBundleWithQueue(bundleName: string): Promise<assetManager.Bundle> {
    return new Promise((resolve, reject) => {
        const task = async () => {
            try {
                // 等待直到有可用的下载槽位
                while (this.activeDownloads >= this.MAX_CONCURRENT_DOWNLOADS) {
                    await new Promise(wait => setTimeout(wait, 100));
                }
                
                this.activeDownloads++;
                
                assetManager.loadBundle(bundleName, (err, bundle) => {
                    this.activeDownloads--;
                    
                    if (err) {
                        console.error(`[PreloadManager] Bundle '${bundleName}' 加载失败:`, err);
                        reject(err);
                    } else {
                        console.log(`[PreloadManager] Bundle '${bundleName}' 加载成功`);
                        this.loadedBundles.set(bundleName, bundle);
                        resolve(bundle);
                    }
                    
                    // 处理队列中的下一个任务
                    if (this.downloadQueue.length > 0) {
                        const nextTask = this.downloadQueue.shift();
                        if (nextTask) {
                            nextTask().catch(console.error);
                        }
                    }
                });
            } catch (error) {
                this.activeDownloads--;
                reject(error);
            }
        };
        
        // 添加到队列
        if (this.activeDownloads >= this.MAX_CONCURRENT_DOWNLOADS) {
            this.downloadQueue.push(task);
        } else {
            task().catch(console.error);
        }
    });
}
```

#### 2. 改造 GameApp，使用 PreloadManager

修改 `GameApp.loadRemoteAssets()`：

```typescript
/**
 * 动态加载远程 Asset Bundle 资源
 */
private async loadRemoteAssets(): Promise<void> {
    try {
        const preloadManager = PreloadManager.getInstance();
        
        // 微信环境：串行加载，避免 429
        if (typeof wx !== 'undefined') {
            console.log('[GameApp] 微信环境：串行加载远程资源');
            
            // 先确保 bg Bundle 已加载
            const bgBundle = await preloadManager.ensureBundleLoaded('bg');
            const spriteFrame = await this.loadSpriteFrameFromBundle(bgBundle, 'game_scene_bg/spriteFrame');
            if (this.backgroundSprite && spriteFrame) {
                this.backgroundSprite.spriteFrame = spriteFrame;
            }
            
            // 等待冷却
            await new Promise(resolve => setTimeout(resolve, 200));
            
            // 再确保 words Bundle 已加载（Loading 阶段应该已加载，这里只是确认）
            await preloadManager.ensureBundleLoaded('words');
        } else {
            // 浏览器环境：保留并发
            console.log('[GameApp] 浏览器环境：并发加载远程资源');
            await Promise.all([
                this.loadBackground(),
                this.ensureWordsBundle()
            ]);
        }
    } catch (error) {
        console.error('[GameApp] 远程资源加载失败:', error);
    }
}

private async loadBackground(): Promise<void> {
    const preloadManager = PreloadManager.getInstance();
    const bgBundle = await preloadManager.ensureBundleLoaded('bg');
    const spriteFrame = await this.loadSpriteFrameFromBundle(bgBundle, 'game_scene_bg/spriteFrame');
    if (this.backgroundSprite && spriteFrame) {
        this.backgroundSprite.spriteFrame = spriteFrame;
    }
}

private async ensureWordsBundle(): Promise<void> {
    const preloadManager = PreloadManager.getInstance();
    await preloadManager.ensureBundleLoaded('words');
}

private loadSpriteFrameFromBundle(bundle: assetManager.Bundle, path: string): Promise<SpriteFrame> {
    return new Promise((resolve, reject) => {
        bundle.load(path, SpriteFrame, (err, spriteFrame) => {
            if (err) {
                reject(err);
            } else {
                resolve(spriteFrame);
            }
        });
    });
}
```

#### 3. 改造 GlossSheet，使用 PreloadManager

修改 `GlossSheet.loadModalResources()`：

```typescript
private async loadModalResources(): Promise<void> {
    try {
        const preloadManager = PreloadManager.getInstance();
        
        // 确保 modal Bundle 已加载
        const modalBundle = await preloadManager.ensureBundleLoaded('modal');
        
        // 加载弹窗背景
        if (this.panel) {
            const spriteFrame = await this.loadSpriteFrameFromBundle(modalBundle, 'pop_card/spriteFrame');
            const sprite = this.panel.getComponent(Sprite);
            if (sprite && spriteFrame) {
                sprite.spriteFrame = spriteFrame;
                sprite.type = Sprite.Type.SLICED;
            }
        }
        
        // 加载收藏按钮图标（本地 resources）
        if (this.starButton) {
            try {
                const starIcon = await this.loadSpriteFrame('badges/reward_star');
                if (starIcon) {
                    const sprite = this.starButton.getComponent(Sprite);
                    if (sprite) {
                        sprite.spriteFrame = starIcon;
                    }
                }
            } catch (error) {
                console.warn('[GlossSheet] 无法加载星星图标，使用默认显示');
            }
        }
    } catch (error) {
        console.warn('[GlossSheet] 弹窗资源加载失败', error);
    }
}

private loadSpriteFrameFromBundle(bundle: assetManager.Bundle, path: string): Promise<SpriteFrame> {
    return new Promise((resolve, reject) => {
        bundle.load(path, SpriteFrame, (err, spriteFrame) => {
            if (err) {
                reject(err);
            } else {
                resolve(spriteFrame);
            }
        });
    });
}

private loadSpriteFrame(path: string): Promise<SpriteFrame> {
    return new Promise((resolve, reject) => {
        resources.load(path, SpriteFrame, (err, spriteFrame) => {
            if (err) {
                reject(err);
            } else {
                resolve(spriteFrame);
            }
        });
    });
}
```

#### 4. 优化 StackGameApp 的初始化

修改 `StackGameApp.onLoad()`，复用已加载的 bundle：

```typescript
protected async onLoad(): Promise<void> {
    // ... 其他初始化代码 ...
    
    // 初始化单词验证系统
    try {
        await this.validationManager.initialize();
    } catch (error) {
        console.error('[StackGameApp] 单词验证系统初始化失败:', error);
    }
    
    // 检查词库是否已加载（Loading 阶段应该已完成）
    const glossService = GlossService.getInstance();
    const loadStatus = glossService.getLoadStatus();
    
    if (!loadStatus.core) {
        console.warn('[StackGameApp] 词库未加载，执行降级加载...');
        try {
            // 使用 PreloadManager 确保 words Bundle 已加载
            const preloadManager = PreloadManager.getInstance();
            await preloadManager.ensureBundleLoaded('words');
            
            // 然后加载词库数据
            await glossService.load(false);
        } catch (error) {
            console.error('[StackGameApp] 词库降级加载失败:', error);
        }
    }
    
    // 加载远程资源（背景图）
    await this.loadRemoteAssets();
    
    // ... 其余初始化代码 ...
}

private async loadRemoteAssets(): Promise<void> {
    try {
        const preloadManager = PreloadManager.getInstance();
        
        // 确保 bg Bundle 已加载
        const bgBundle = await preloadManager.ensureBundleLoaded('bg');
        
        // 加载背景图
        const spriteFrame = await this.loadSpriteFrameFromBundle(bgBundle, 'game_scene_bg/spriteFrame');
        if (this.backgroundSprite && spriteFrame) {
            this.backgroundSprite.spriteFrame = spriteFrame;
        }
    } catch (error) {
        console.error('[StackGameApp] 加载场景背景图失败:', error);
        // 背景图加载失败不影响游戏运行
    }
}

private loadSpriteFrameFromBundle(bundle: assetManager.Bundle, path: string): Promise<SpriteFrame> {
    return new Promise((resolve, reject) => {
        bundle.load(path, SpriteFrame, (err, spriteFrame) => {
            if (err) {
                reject(err);
            } else {
                resolve(spriteFrame);
            }
        });
    });
}
```

#### 5. 优化 LocalDictionary 和 BloomFilter

修改 `LocalDictionary.loadCoreDict()` 和 `loadExtendedDict()`，复用已加载的 bundle：

```typescript
private async loadCoreDict(): Promise<void> {
    try {
        // 使用 PreloadManager 确保 words Bundle 已加载
        const preloadManager = PreloadManager.getInstance();
        const bundle = await preloadManager.ensureBundleLoaded('words');
        
        const mergeJson = (asset?: JsonAsset | null) => {
            if (asset && asset.json) {
                for (const [word, definition] of Object.entries(asset.json)) {
                    if (typeof definition === 'string') {
                        this.coreDict.set(String(word).toUpperCase(), definition);
                    }
                }
            }
        };
        
        // 加载核心词库
        await new Promise<void>((resolve) => {
            bundle.load('zh_gloss', JsonAsset, (loadErr, asset) => {
                if (loadErr) {
                    console.warn('[LocalDictionary] 核心词库释义加载失败');
                } else {
                    mergeJson(asset);
                }
                
                // 加载自定义词库
                bundle.load('zh_gloss_custom', JsonAsset, (cusErr, cusAsset) => {
                    if (!cusErr) {
                        mergeJson(cusAsset);
                    }
                    resolve();
                });
            });
        });
    } catch (error) {
        console.warn('[LocalDictionary] 词库加载失败:', error);
    }
}

private async loadExtendedDict(): Promise<void> {
    try {
        // 使用 PreloadManager 确保 words Bundle 已加载
        const preloadManager = PreloadManager.getInstance();
        const bundle = await preloadManager.ensureBundleLoaded('words');
        
        // 加载扩展词库
        await new Promise<void>((resolve) => {
            bundle.load('words_extended', JsonAsset, (wordsErr, wordsAsset) => {
                if (wordsErr) {
                    console.warn('[LocalDictionary] 扩展词库加载失败');
                    resolve();
                    return;
                }
                
                if (wordsAsset && wordsAsset.json) {
                    const jsonData = wordsAsset.json as any;
                    if (jsonData.by_len) {
                        for (const len in jsonData.by_len) {
                            const words = jsonData.by_len[len];
                            if (Array.isArray(words)) {
                                words.forEach((word: string) => {
                                    this.extendedDict.add(word.toUpperCase());
                                });
                            }
                        }
                    }
                }
                
                // 加载扩展词库释义
                bundle.load('zh_gloss_extended', JsonAsset, (glossErr, glossAsset) => {
                    if (!glossErr && glossAsset && glossAsset.json) {
                        for (const [word, definition] of Object.entries(glossAsset.json)) {
                            if (typeof definition === 'string') {
                                if (!this.coreDict.has(word.toUpperCase())) {
                                    this.coreDict.set(word.toUpperCase(), definition);
                                }
                            }
                        }
                    }
                    resolve();
                });
            });
        });
    } catch (error) {
        console.warn('[LocalDictionary] 扩展词库加载失败:', error);
    }
}
```

同理修改 `BloomFilter`：

```typescript
export async function loadBloomFromBundle(): Promise<void> {
    try {
        // 使用 PreloadManager 确保 words Bundle 已加载
        const preloadManager = PreloadManager.getInstance();
        const bundle = await preloadManager.ensureBundleLoaded('words');
        
        return new Promise((resolve) => {
            bundle.load('bloom_filter', JsonAsset, (err, asset) => {
                if (err || !asset || !asset.json) {
                    console.warn('[BloomFilter] Bloom filter 加载失败');
                    resolve();
                    return;
                }
                
                try {
                    const data = asset.json as BloomData;
                    initializeBloom(data);
                    console.log('[BloomFilter] Bloom filter 加载成功');
                } catch (e) {
                    console.warn('[BloomFilter] Bloom filter 解析失败:', e);
                }
                resolve();
            });
        });
    } catch (error) {
        console.warn('[BloomFilter] 加载失败:', error);
    }
}
```

### 修复方案的优势

1. **改动最小**: 不需要新建 `BundleRequestQueue` 系统，只需扩展 `PreloadManager`
2. **复用现有优化**: 充分利用已有的并发控制、重试机制和平台差异化策略
3. **缓存优先**: 所有调用先检查缓存，避免重复下载
4. **渐进式加载**: 不需要把所有字典数据都放回 Loading 阶段，保持首屏加载速度
5. **易于维护**: 所有 bundle 加载逻辑集中在 `PreloadManager`，便于调试和优化

### 验证与回归

1. **在微信开发者工具中验证**:
   - 在 `PreloadManager.ensureBundleLoaded` 中添加日志，记录每次调用的时间戳
   - 监控 `wx.downloadFile` 的并发数量，确保始终低于 10
   - 全链路测试: Loading → MainMenu → Game/Stack → Result

2. **真机测试**:
   - 使用微信开发者工具的"真机调试"功能
   - 在 Network 面板监控请求，确认无 429 错误
   - 测试不同网络环境（WiFi、4G、3G）

3. **性能测试**:
   - 记录主场景初始化时间，确保不显著增加
   - 监控内存占用，确保没有泄漏
   - 测试低端机型，确保流畅性

实施完以上步骤后，所有远程 bundle 调用都经过 `PreloadManager` 的统一调度，主场景再也不会暴发 429。